name: Joomla 5/6 admin batch 225

on:
  push:
    branches: [joomla5-6]
    paths:
      - 'admin/src/View/Sportsmanagement/HtmlView.php'
      - 'admin/src/View/Sportsmanagements/HtmlView.php'
      - '.github/workflows/joomla5-6-admin-batch225.yml'
  pull_request:
    branches: [joomla5-6, master]
    paths:
      - 'admin/src/View/Sportsmanagement/HtmlView.php'
      - 'admin/src/View/Sportsmanagements/HtmlView.php'
      - '.github/workflows/joomla5-6-admin-batch225.yml'

permissions:
  contents: read

jobs:
  compatibility:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
      - name: Validate PHP and required headers
        shell: bash
        run: |
          set -euo pipefail
          files=(admin/src/View/Sportsmanagement/HtmlView.php admin/src/View/Sportsmanagements/HtmlView.php)
          for file in "${files[@]}"; do
            php -l "$file"
            for tag in '@version' '@author' '@copyright' '@license'; do
              grep -Fq -- "$tag" "$file"
            done
          done
      - name: Validate Joomla 5/6 contracts
        shell: bash
        run: |
          set -euo pipefail
          ! grep -Fq -- '$this->getDocument()->getWebAssetManager()' admin/src/View/Sportsmanagement/HtmlView.php
          grep -Fq -- 'Factory::getContainer()->get(AdministratorApplication::class)' admin/src/View/Sportsmanagements/HtmlView.php
          ! grep -Fq -- 'Factory::getApplication()' admin/src/View/Sportsmanagements/HtmlView.php
          ! grep -Fq -- '$this->getDocument()->getWebAssetManager()' admin/src/View/Sportsmanagements/HtmlView.php
          echo 'Joomla 5/6 batch 225 contracts OK.'
