name: Joomla 5/6 admin controller bridges batch 381

on:
  push:
    branches: [joomla5-6]
    paths:
      - 'admin/controllers/smquotetxt.php'
      - 'admin/controllers/template.php'
      - 'admin/controllers/match.php'
      - 'admin/controllers/agegroup.php'
      - 'admin/controllers/statistic.php'
      - 'admin/controllers/jlextassociation.php'
      - '.github/workflows/joomla5-6-admin-controller-bridges-batch381.yml'
  pull_request:
    branches: [joomla5-6, master]
    paths:
      - 'admin/controllers/smquotetxt.php'
      - 'admin/controllers/template.php'
      - 'admin/controllers/match.php'
      - 'admin/controllers/agegroup.php'
      - 'admin/controllers/statistic.php'
      - 'admin/controllers/jlextassociation.php'
      - '.github/workflows/joomla5-6-admin-controller-bridges-batch381.yml'

permissions:
  contents: read

jobs:
  compatibility:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
      - name: Validate administrator controller bridges
        shell: bash
        run: |
          set -euo pipefail
          files=(
            admin/controllers/smquotetxt.php
            admin/controllers/template.php
            admin/controllers/match.php
            admin/controllers/agegroup.php
            admin/controllers/statistic.php
            admin/controllers/jlextassociation.php
          )
          for file in "${files[@]}"; do
            php -l "$file"
            for tag in '@version' '@author' '@copyright' '@license'; do
              grep -Fq -- "$tag" "$file"
            done
            grep -Fq -- '@version    5.6.0' "$file"
            grep -Fq -- "\\defined('_JEXEC') or die;" "$file"
            ! grep -Fq -- 'Restricted access' "$file"
            grep -Fq -- 'class_alias(' "$file"
            grep -Fq -- 'throw new \\RuntimeException' "$file"
          done
