name: Joomla 5/6 compatibility batch 465

on:
  push:
    branches: [joomla5-6]
    paths:
      - 'site/models/teamstats.php'
      - 'admin/models/predictiongroup.php'
      - 'modules/mod_sportsmanagement_rquotes/helper.php'
      - '.github/workflows/joomla5-6-compatibility-batch465.yml'
  pull_request:
    branches: [joomla5-6, master]
    paths:
      - 'site/models/teamstats.php'
      - 'admin/models/predictiongroup.php'
      - 'modules/mod_sportsmanagement_rquotes/helper.php'
      - '.github/workflows/joomla5-6-compatibility-batch465.yml'

permissions:
  contents: read

jobs:
  compatibility:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
      - name: Validate site, administrator and module compatibility
        shell: bash
        run: |
          set -euo pipefail
          files=(
            site/models/teamstats.php
            admin/models/predictiongroup.php
            modules/mod_sportsmanagement_rquotes/helper.php
          )
          for file in "${files[@]}"; do
            php -l "$file"
            for tag in '@version' '@author' '@copyright' '@license'; do
              grep -Fq -- "$tag" "$file"
            done
            grep -Fq -- '@version    5.6.0' "$file"
          done

          grep -Fq -- 'SportsManagementDatabaseResolver.php' site/models/teamstats.php
          grep -Fq -- 'SportsManagementSiteApplicationResolver.php' site/models/teamstats.php
          grep -Fq -- 'throw new \RuntimeException' site/models/teamstats.php

          grep -Fq -- 'is_file($nativeModel)' admin/models/predictiongroup.php
          grep -Fq -- 'throw new \RuntimeException' admin/models/predictiongroup.php
          ! grep -Fq -- 'require_once JPATH_ADMINISTRATOR' admin/models/predictiongroup.php

          grep -Fq -- "if (!class_exists('modRquotesHelper', false))" modules/mod_sportsmanagement_rquotes/helper.php
          grep -Fq -- 'final class modRquotesHelper' modules/mod_sportsmanagement_rquotes/helper.php
