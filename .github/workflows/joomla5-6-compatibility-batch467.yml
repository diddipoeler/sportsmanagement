name: Joomla 5/6 compatibility batch 467

on:
  push:
    branches: [joomla5-6]
    paths:
      - 'site/models/matrix.php'
      - 'admin/models/predictionproject.php'
      - 'admin/models/predictionround.php'
      - '.github/workflows/joomla5-6-compatibility-batch467.yml'
  pull_request:
    branches: [joomla5-6, master]
    paths:
      - 'site/models/matrix.php'
      - 'admin/models/predictionproject.php'
      - 'admin/models/predictionround.php'
      - '.github/workflows/joomla5-6-compatibility-batch467.yml'

permissions:
  contents: read

jobs:
  compatibility:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
      - name: Validate Joomla 5/6 compatibility
        shell: bash
        run: |
          set -euo pipefail
          files=(
            site/models/matrix.php
            admin/models/predictionproject.php
            admin/models/predictionround.php
          )
          for file in "${files[@]}"; do
            php -l "$file"
            for tag in '@version' '@author' '@copyright' '@license'; do
              grep -Fq -- "$tag" "$file"
            done
            grep -Fq -- '@version    5.6.0' "$file"
          done

          grep -Fq -- 'SportsManagementDatabaseResolver.php' site/models/matrix.php
          grep -Fq -- 'SportsManagementSiteApplicationResolver.php' site/models/matrix.php
          grep -Fq -- 'is_file($nativeModel)' site/models/matrix.php
          ! grep -Fq -- 'require_once JPATH_SITE' site/models/matrix.php

          grep -Fq -- 'is_file($nativeModel)' admin/models/predictionproject.php
          grep -Fq -- 'throw new \RuntimeException' admin/models/predictionproject.php
          ! grep -Fq -- 'require_once JPATH_ADMINISTRATOR' admin/models/predictionproject.php

          grep -Fq -- 'is_file($nativeModel)' admin/models/predictionround.php
          grep -Fq -- 'throw new \RuntimeException' admin/models/predictionround.php
          ! grep -Fq -- 'require_once JPATH_ADMINISTRATOR' admin/models/predictionround.php
