name: Joomla 5/6 events ranking entry dependencies batch 417

on:
  push:
    branches: [joomla5-6]
    paths:
      - 'modules/mod_sportsmanagement_eventsranking/mod_sportsmanagement_eventsranking.php'
      - '.github/workflows/joomla5-6-events-ranking-entry-dependencies-batch417.yml'
  pull_request:
    branches: [joomla5-6, master]
    paths:
      - 'modules/mod_sportsmanagement_eventsranking/mod_sportsmanagement_eventsranking.php'
      - '.github/workflows/joomla5-6-events-ranking-entry-dependencies-batch417.yml'

permissions:
  contents: read

jobs:
  compatibility:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
      - name: Validate EventsRanking entry dependencies
        shell: bash
        run: |
          set -euo pipefail
          file=modules/mod_sportsmanagement_eventsranking/mod_sportsmanagement_eventsranking.php
          php -l "$file"
          for tag in '@version' '@author' '@copyright' '@license'; do
            grep -Fq -- "$tag" "$file"
          done
          grep -Fq -- '@version    5.6.0' "$file"
          grep -Fq -- 'SiteRouteHelper::class' "$file"
          grep -Fq -- 'SportsManagementDatabaseResolver::class' "$file"
          grep -Fq -- 'SportsManagementSiteApplicationResolver::class' "$file"
          grep -Fq -- 'EventsRankingHelper::class => __DIR__' "$file"
          grep -Fq -- 'throw new \RuntimeException' "$file"
