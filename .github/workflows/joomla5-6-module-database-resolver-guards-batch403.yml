name: Joomla 5/6 module database resolver guards batch 403

on:
  push:
    branches: [joomla5-6]
    paths:
      - 'modules/mod_sportsmanagement_teamstats_ranking/helper.php'
      - 'modules/mod_sportsmanagement_sports_type_statistics/helper.php'
      - '.github/workflows/joomla5-6-module-database-resolver-guards-batch403.yml'
  pull_request:
    branches: [joomla5-6, master]
    paths:
      - 'modules/mod_sportsmanagement_teamstats_ranking/helper.php'
      - 'modules/mod_sportsmanagement_sports_type_statistics/helper.php'
      - '.github/workflows/joomla5-6-module-database-resolver-guards-batch403.yml'

permissions:
  contents: read

jobs:
  compatibility:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
      - name: Validate module database resolver guards
        shell: bash
        run: |
          set -euo pipefail
          files=(
            modules/mod_sportsmanagement_teamstats_ranking/helper.php
            modules/mod_sportsmanagement_sports_type_statistics/helper.php
          )
          for file in "${files[@]}"; do
            php -l "$file"
            for tag in '@version' '@author' '@copyright' '@license'; do
              grep -Fq -- "$tag" "$file"
            done
            grep -Fq -- '@version    5.6.0' "$file"
            grep -Fq -- 'SportsManagementDatabaseResolver::class' "$file"
            grep -Fq -- 'throw new \RuntimeException' "$file"
          done
          grep -Fq -- 'TeamStatsRankingHelper::class => __DIR__' modules/mod_sportsmanagement_teamstats_ranking/helper.php
          grep -Fq -- 'SiteRouteHelper::class' modules/mod_sportsmanagement_teamstats_ranking/helper.php
          grep -Fq -- 'SportsTypeStatisticsHelper::class => __DIR__' modules/mod_sportsmanagement_sports_type_statistics/helper.php
