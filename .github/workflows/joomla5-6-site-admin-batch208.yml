name: Joomla 5/6 site admin batch 208

on:
  push:
    branches: [joomla5-6]
    paths:
      - 'site/src/View/Editclub/HtmlView.php'
      - 'admin/src/View/Currentseasons/HtmlView.php'
      - '.github/workflows/joomla5-6-site-admin-batch208.yml'
  pull_request:
    branches: [joomla5-6, master]
    paths:
      - 'site/src/View/Editclub/HtmlView.php'
      - 'admin/src/View/Currentseasons/HtmlView.php'
      - '.github/workflows/joomla5-6-site-admin-batch208.yml'

permissions:
  contents: read

jobs:
  compatibility:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
      - name: Validate PHP and required headers
        shell: bash
        run: |
          set -euo pipefail
          files=(site/src/View/Editclub/HtmlView.php admin/src/View/Currentseasons/HtmlView.php)
          for file in "${files[@]}"; do
            php -l "$file"
            for tag in '@version' '@author' '@copyright' '@license'; do
              grep -Fq -- "$tag" "$file"
            done
          done
      - name: Validate Joomla 5/6 contracts
        shell: bash
        run: |
          set -euo pipefail
          grep -Fq -- 'if ($document instanceof HtmlDocument)' site/src/View/Editclub/HtmlView.php
          ! grep -Fq -- '$this->getDocument()->getWebAssetManager()' site/src/View/Editclub/HtmlView.php
          grep -Fq -- 'if ($document instanceof HtmlDocument)' admin/src/View/Currentseasons/HtmlView.php
          ! grep -Fq -- '$this->getDocument()->getWebAssetManager()' admin/src/View/Currentseasons/HtmlView.php
          echo 'Joomla 5/6 batch 208 contracts OK.'
