name: Joomla 5/6 site admin batch 210

on:
  push:
    branches: [joomla5-6]
    paths:
      - 'site/src/View/Imagehandler/HtmlView.php'
      - 'admin/src/View/Github/HtmlView.php'
      - '.github/workflows/joomla5-6-site-admin-batch210.yml'
  pull_request:
    branches: [joomla5-6, master]
    paths:
      - 'site/src/View/Imagehandler/HtmlView.php'
      - 'admin/src/View/Github/HtmlView.php'
      - '.github/workflows/joomla5-6-site-admin-batch210.yml'

permissions:
  contents: read

jobs:
  compatibility:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
      - name: Validate PHP and required headers
        shell: bash
        run: |
          set -euo pipefail
          files=(site/src/View/Imagehandler/HtmlView.php admin/src/View/Github/HtmlView.php)
          for file in "${files[@]}"; do
            php -l "$file"
            for tag in '@version' '@author' '@copyright' '@license'; do
              grep -Fq -- "$tag" "$file"
            done
          done
      - name: Validate Joomla 5/6 contracts
        shell: bash
        run: |
          set -euo pipefail
          for tag in '@version' '@author' '@copyright' '@license'; do
            grep -Fq -- "$tag" site/src/View/Imagehandler/HtmlView.php
          done
          grep -Fq -- 'use Joomla\CMS\Document\HtmlDocument;' admin/src/View/Github/HtmlView.php
          grep -Fq -- 'if ($document instanceof HtmlDocument)' admin/src/View/Github/HtmlView.php
          ! grep -Fq -- '$app->getDocument()->getWebAssetManager()' admin/src/View/Github/HtmlView.php
          echo 'Joomla 5/6 batch 210 contracts OK.'
