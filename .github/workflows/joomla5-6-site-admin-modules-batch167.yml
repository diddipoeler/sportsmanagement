name: Joomla 5/6 site admin modules batch 167

on:
  push:
    branches: [joomla5-6]
    paths:
      - 'site/views/teamplan/view.html.php'
      - 'admin/src/View/Installhelper/HtmlView.php'
      - 'modules/mod_sportsmanagement_firstleagueoverview/helper.php'
      - '.github/workflows/joomla5-6-site-admin-modules-batch167.yml'
  pull_request:
    branches: [joomla5-6, master]
    paths:
      - 'site/views/teamplan/view.html.php'
      - 'admin/src/View/Installhelper/HtmlView.php'
      - 'modules/mod_sportsmanagement_firstleagueoverview/helper.php'
      - '.github/workflows/joomla5-6-site-admin-modules-batch167.yml'

permissions:
  contents: read

jobs:
  compatibility:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none

      - name: Validate PHP and required headers
        shell: bash
        run: |
          set -euo pipefail
          files=(
            site/views/teamplan/view.html.php
            admin/src/View/Installhelper/HtmlView.php
            modules/mod_sportsmanagement_firstleagueoverview/helper.php
          )
          for file in "${files[@]}"; do
            php -l "$file"
            for tag in '@version' '@author' '@copyright' '@license'; do
              grep -Fq -- "$tag" "$file"
            done
          done

      - name: Validate Joomla 5/6 contracts
        shell: bash
        run: |
          set -euo pipefail

          grep -Fq -- 'Legacy compatibility bridge for the native Joomla 5/6 frontend Teamplan view.' site/views/teamplan/view.html.php
          grep -Fq -- "class_alias(HtmlView::class, 'sportsmanagementViewTeamPlan')" site/views/teamplan/view.html.php

          grep -Fq -- 'Factory::getContainer()->get(AdministratorApplication::class)' admin/src/View/Installhelper/HtmlView.php
          ! grep -Fq -- 'Factory::getApplication()' admin/src/View/Installhelper/HtmlView.php
          grep -Fq -- 'SportsManagement Installhelper view requires the Joomla administrator application.' admin/src/View/Installhelper/HtmlView.php

          grep -Fq -- "instanceof SiteApplication || !\$app->isClient('site')" modules/mod_sportsmanagement_firstleagueoverview/helper.php
          grep -Fq -- 'SportsManagement FirstLeagueOverview legacy bridge requires the Joomla site application.' modules/mod_sportsmanagement_firstleagueoverview/helper.php

          echo 'Joomla 5/6 batch 167 contracts OK.'
