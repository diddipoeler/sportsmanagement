name: Joomla 5/6 site admin modules batch 179

on:
  push:
    branches: [joomla5-6]
    paths:
      - 'site/views/scoresheet/view.html.php'
      - 'admin/src/View/Predictions/HtmlView.php'
      - 'modules/mod_sportsmanagement_uefawertung/helper.php'
      - '.github/workflows/joomla5-6-site-admin-modules-batch179.yml'
  pull_request:
    branches: [joomla5-6, master]
    paths:
      - 'site/views/scoresheet/view.html.php'
      - 'admin/src/View/Predictions/HtmlView.php'
      - 'modules/mod_sportsmanagement_uefawertung/helper.php'
      - '.github/workflows/joomla5-6-site-admin-modules-batch179.yml'

permissions:
  contents: read

jobs:
  compatibility:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none

      - name: Validate PHP and required headers
        shell: bash
        run: |
          set -euo pipefail
          files=(
            site/views/scoresheet/view.html.php
            admin/src/View/Predictions/HtmlView.php
            modules/mod_sportsmanagement_uefawertung/helper.php
          )
          for file in "${files[@]}"; do
            php -l "$file"
            for tag in '@version' '@author' '@copyright' '@license'; do
              grep -Fq -- "$tag" "$file"
            done
          done

      - name: Validate Joomla 5/6 contracts
        shell: bash
        run: |
          set -euo pipefail

          grep -Fq -- "\\defined('_JEXEC') or die;" site/views/scoresheet/view.html.php
          ! grep -Fq -- "die('Restricted access')" site/views/scoresheet/view.html.php

          grep -Fq -- 'Native Joomla 5/6 administrator prediction management dashboard.' admin/src/View/Predictions/HtmlView.php

          grep -Fq -- 'SportsManagement UEFA ranking legacy helper requires the Joomla site application.' modules/mod_sportsmanagement_uefawertung/helper.php
          grep -Fq -- "instanceof SiteApplication || !\$app->isClient('site')" modules/mod_sportsmanagement_uefawertung/helper.php

          echo 'Joomla 5/6 batch 179 contracts OK.'
