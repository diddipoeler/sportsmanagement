name: Joomla 5/6 site admin modules batch 181

on:
  push:
    branches: [joomla5-6]
    paths:
      - 'site/views/rankingplayerbillard/view.html.php'
      - 'admin/src/View/Predictiongroup/HtmlView.php'
      - 'modules/mod_sportsmanagement_act_season/helper.php'
      - '.github/workflows/joomla5-6-site-admin-modules-batch181.yml'
  pull_request:
    branches: [joomla5-6, master]
    paths:
      - 'site/views/rankingplayerbillard/view.html.php'
      - 'admin/src/View/Predictiongroup/HtmlView.php'
      - 'modules/mod_sportsmanagement_act_season/helper.php'
      - '.github/workflows/joomla5-6-site-admin-modules-batch181.yml'

permissions:
  contents: read

jobs:
  compatibility:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none

      - name: Validate PHP and required headers
        shell: bash
        run: |
          set -euo pipefail
          files=(
            site/views/rankingplayerbillard/view.html.php
            admin/src/View/Predictiongroup/HtmlView.php
            modules/mod_sportsmanagement_act_season/helper.php
          )
          for file in "${files[@]}"; do
            php -l "$file"
            for tag in '@version' '@author' '@copyright' '@license'; do
              grep -Fq -- "$tag" "$file"
            done
          done

      - name: Validate Joomla 5/6 contracts
        shell: bash
        run: |
          set -euo pipefail

          grep -Fq -- '@version    5.6.0' site/views/rankingplayerbillard/view.html.php
          grep -Fq -- "\\defined('_JEXEC') or die;" site/views/rankingplayerbillard/view.html.php
          ! grep -Fq -- "die('Restricted access')" site/views/rankingplayerbillard/view.html.php

          grep -Fq -- 'private static function administratorApplication(): AdministratorApplication' admin/src/View/Predictiongroup/HtmlView.php
          grep -Fq -- 'Factory::getContainer()->get(AdministratorApplication::class)' admin/src/View/Predictiongroup/HtmlView.php
          ! grep -Fq -- 'Factory::getApplication()' admin/src/View/Predictiongroup/HtmlView.php

          grep -Fq -- 'use Joomla\\CMS\\Application\\SiteApplication;' modules/mod_sportsmanagement_act_season/helper.php
          grep -Fq -- "instanceof SiteApplication || !\$app->isClient('site')" modules/mod_sportsmanagement_act_season/helper.php

          echo 'Joomla 5/6 batch 181 contracts OK.'
